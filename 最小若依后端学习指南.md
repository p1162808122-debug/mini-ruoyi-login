# 从一次登录看懂整个后端：最小 RuoYi 学习指南

> 配套项目：[`mini-ruoyi-login`](./mini-ruoyi-login/)  
> 原项目：[`RuoYi-master`](./RuoYi-master/)（没有修改）  
> 教学账号：`admin` / `admin123`

这个项目不是要重新发明一套技术，而是把 RuoYi 的主干单独放大。你只需要先看懂这一条线：

```text
浏览器 → Shiro 过滤器 → Controller → Shiro → Realm → Service → Mapper → H2 数据库
浏览器 ← JSON ← Controller ← Shiro ← Realm ← Service ← Mapper ← H2 查询结果
```

看完后，你应该能独立说清楚四件事：

1. 用户名和密码怎样从页面进入 Java。
2. `subject.login(...)` 为什么会走到 Realm、Service、Mapper 和数据库。
3. 数据库的一行数据怎样变成 `User` 对象，又怎样原路返回。
4. 登录成功后，Session 和权限怎样保护唯一的主页接口。

---

## 1. 先认文件：每个文件是做什么的

先不要急着逐行看代码。你第一次阅读时，只需建立“谁负责什么”的地图。

```text
mini-ruoyi-login/
├── pom.xml
├── run.sh
├── javac21.sh
├── .gitignore
└── src/main/
    ├── java/com/pengzhipeng/miniruoyi/
    │   ├── MiniRuoYiApplication.java
    │   ├── common/ApiResponse.java
    │   ├── config/ShiroConfig.java
    │   ├── controller/AuthController.java
    │   ├── domain/User.java
    │   ├── dto/ProfileResponse.java
    │   ├── mapper/UserMapper.java
    │   ├── security/JsonAuthenticationFilter.java
    │   ├── security/UserRealm.java
    │   └── service/UserService.java
    └── resources/
        ├── application.yml
        ├── schema.sql
        ├── data.sql
        ├── mapper/UserMapper.xml
        └── static/index.html
```

### 1.1 启动和依赖文件

| 文件 | 一句话职责 | 谁会读取它 | 第一次重点看什么 |
|---|---|---|---|
| [`pom.xml`](./mini-ruoyi-login/pom.xml) | 项目的“材料清单”，声明 Spring Boot、Shiro、MyBatis、H2 等依赖 | Maven | `<dependencies>` 中的 5 类框架 |
| [`run.sh`](./mini-ruoyi-login/run.sh) | 一条命令启动项目，并使用服务器已有 Maven | Linux Shell | 最后一行 `spring-boot:run` |
| [`javac21.sh`](./mini-ruoyi-login/javac21.sh) | 适配这台服务器的裁剪版 Java 21，让 Maven 能调用编译器 | Maven 编译插件 | 它只是环境适配，不在登录流程中 |
| [`.gitignore`](./mini-ruoyi-login/.gitignore) | 告诉 Git 忽略 `target/`、日志等生成文件 | Git | 它不参与程序运行 |
| [`MiniRuoYiApplication.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/MiniRuoYiApplication.java) | Java 程序总入口，启动 Spring 容器和内置 Tomcat | `java` 命令 | `main` 方法和两个注解 |

`pom.xml` 里依赖很多 JAR，但你当前只需要记住名称和作用：

- Spring Boot WebMVC：接收 HTTP 请求、调用 Controller、把 Java 对象转成 JSON。
- Shiro：登录认证、Session、角色和权限检查。
- MyBatis：让 Java 的 Mapper 方法执行 SQL。
- H2：教学用内存数据库，不需要另外安装 MySQL。
- AspectJ：让 `@RequiresPermissions` 这类权限注解真正生效。

### 1.2 登录和权限文件

| 文件 | 一句话职责 | 谁调用它 | 它调用谁 |
|---|---|---|---|
| [`ShiroConfig.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/config/ShiroConfig.java) | 把 Realm、密码规则、Session、过滤路径组装给 Shiro | Spring 启动时读取 | 创建 `UserRealm`、过滤器和 SecurityManager |
| [`JsonAuthenticationFilter.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/security/JsonAuthenticationFilter.java) | 未登录访问 `/api/**` 时返回 401 JSON | Shiro 请求过滤链 | 不进入 Controller，直接写响应 |
| [`UserRealm.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/security/UserRealm.java) | Shiro 与我们自己的用户数据之间的桥 | Shiro | 调用 `UserService`；返回认证信息和权限 |
| [`AuthController.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/controller/AuthController.java) | 接收登录、读取资料、退出三个 HTTP 请求 | Spring MVC | 调用 Shiro 的 `Subject` |

最容易误解的一点：`AuthController` **没有直接调用 `UserRealm`**。

```java
subject.login(new UsernamePasswordToken(username, password));
```

Controller 只把 Token 交给 Shiro。接下来是 Shiro 框架在内部回调 `UserRealm#doGetAuthenticationInfo`。就像前台把来访者交给门卫系统，前台不会自己跑去翻数据库。

### 1.3 主业务和数据库文件

| 文件 | 一句话职责 | 谁调用它 | 它调用谁 |
|---|---|---|---|
| [`UserService.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/service/UserService.java) | 业务层：检查并整理用户名，再安排查询 | `UserRealm` | `UserMapper` |
| [`UserMapper.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/mapper/UserMapper.java) | 声明“按用户名查用户”这个数据库动作 | `UserService` 调用 MyBatis 生成的 Mapper 代理对象 | 代理对象根据 XML 执行 SQL |
| [`UserMapper.xml`](./mini-ruoyi-login/src/main/resources/mapper/UserMapper.xml) | 写真正执行的 SQL，并说明结果装进哪种对象 | MyBatis | H2 数据库 |
| [`User.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/domain/User.java) | 对应数据库 `app_user` 表的一行完整数据 | MyBatis 创建并填写 | 被 Realm、Shiro 使用 |
| [`ProfileResponse.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/dto/ProfileResponse.java) | 只挑安全字段给前端，不泄露密码哈希和盐 | `AuthController` 创建 | 被装进 `ApiResponse` |
| [`ApiResponse.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/common/ApiResponse.java) | 所有 JSON 的统一外包装，类似 RuoYi 的 `AjaxResult` | Controller | Spring/Jackson 将它变成 JSON |

这里故意没有把 Service 拆成 `IUserService` 和 `UserServiceImpl`。完整版项目常这样拆，方便多个实现；但当前只有一种实现，拆开只会多一个文件，不会增加你对主流程的理解。

### 1.4 配置、数据库和页面文件

| 文件 | 一句话职责 | 什么时候使用 | 删除后会怎样 |
|---|---|---|---|
| [`application.yml`](./mini-ruoyi-login/src/main/resources/application.yml) | 配置端口 8081、H2 连接、Mapper XML 位置和 SQL 日志 | 应用启动时 | 端口和数据库等无法正确配置 |
| [`schema.sql`](./mini-ruoyi-login/src/main/resources/schema.sql) | 创建 `app_user` 表，也就是搭一个空档案柜 | 每次启动时 | Mapper 查询时会报“表不存在” |
| [`data.sql`](./mini-ruoyi-login/src/main/resources/data.sql) | 插入唯一教学账号 `admin` | 建表后自动执行 | 数据库里没有可登录用户 |
| [`index.html`](./mini-ruoyi-login/src/main/resources/static/index.html) | 唯一页面：显示登录区，登录成功后切换为主页区 | 浏览器访问 `/` | 后端还在，但没有可视页面 |

`target/` 是 Maven 编译后自动生成的目录，不是你手写的源码。第一次学习可以完全不看。

### 1.5 第一次真正需要反复看的文件

其他文件是在“搭舞台”，你真正要来回追踪的是这 6 个：

```text
AuthController.java
    ↓ subject.login 触发 Shiro 回调
UserRealm.java
    ↓
UserService.java
    ↓
UserMapper.java
    ↓ namespace + id 对上
UserMapper.xml
    ↓ SQL
data.sql / H2
```

---

## 2. 用一个生活类比先看懂分工

下面一直使用同一个“办事大厅”类比：

| 后端概念 | 生活中的角色 | 在本项目里做什么 |
|---|---|---|
| 浏览器 | 来访者 | 提交用户名、密码，拿回结果 |
| Shiro 过滤器 | 大门门卫 | 未登录不能访问受保护接口 |
| Controller | 前台接待员 | 接请求、交给系统处理、发回回执 |
| Token | 临时登记单 | 暂时装用户名和本次输入的密码 |
| Realm | 身份核验窗口 | 知道应当去哪里查真实用户 |
| Service | 业务办理员 | 处理业务规则并安排数据库查询 |
| Mapper | 档案室翻译员 | 把 Java 方法翻译成 SQL |
| H2 | 档案柜 | 保存用户那一行数据 |
| `User` | 内部完整档案 | 有密码哈希、盐、状态、权限等 |
| `ProfileResponse` | 对外公开证明 | 删除敏感字段后交给来访者 |
| `ApiResponse` | 统一回执信封 | 装状态码、提示、真正数据 |

调用方向用 `→`，`return` 或查询结果返回用 `←`：

```text
去程：页面 → 过滤器 → Controller → Shiro → Realm → Service → Mapper → SQL → H2
回程：页面 ← JSON   ← Controller ← Shiro ← Realm  ← Service ← Mapper ← User ← H2 一行数据
```

---

## 3. 一次成功登录，数据逐站怎样流动

下面固定使用同一份数据：用户名 `admin`，密码 `admin123`。

### 第 1 站：页面把输入变成 HTTP 请求

你点击“登录并跑通后端”后，[`index.html`](./mini-ruoyi-login/src/main/resources/static/index.html) 中的 JavaScript 读取表单：

```javascript
const body = new URLSearchParams(new FormData(form));
```

此时数据从页面上的两个输入框，变成大致这样的请求正文：

```text
username=admin&password=admin123
```

随后浏览器发送：

```text
POST /api/login
Content-Type: application/x-www-form-urlencoded
```

### 第 2 站：Shiro 先看这条路能不能走

任何请求都会先经过 Shiro。它在 [`ShiroConfig.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/config/ShiroConfig.java) 中看到：

```java
chain.put("/api/login", "anon");
chain.put("/api/**", "jsonAuthc");
```

`anon` 意思是匿名也能访问。登录接口必须放行，否则会出现“你必须先登录，才能请求登录”的死循环。

路径匹配有顺序：更具体的 `/api/login` 在前，所以它先匹配并放行；其他 `/api/**` 才要求已登录。

### 第 3 站：Spring 找到 Controller 方法并绑定参数

Spring 根据两个注解找到 [`AuthController#login`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/controller/AuthController.java)：

```java
@PostMapping("/login")
public ResponseEntity<ApiResponse<ProfileResponse>> login(
        @RequestParam String username,
        @RequestParam String password)
```

把方法声明拆开看：

- `public`：其他对象可以调用这个方法。
- `ResponseEntity<ApiResponse<ProfileResponse>>`：返回值类型。最外层能控制 HTTP 状态；中间是统一回执；最里面装公开用户资料。
- `login`：方法名。
- `String username`：参数类型是字符串，参数变量名是 `username`。
- `@RequestParam`：告诉 Spring 从表单请求参数中取值。

此刻调试器里应该看到：

```text
username = "admin"
password = "admin123"
```

### 第 4 站：Controller 创建 Token，交给 Shiro

```java
Subject subject = SecurityUtils.getSubject();
subject.login(new UsernamePasswordToken(username, password));
```

- `Subject` 不是数据库用户，它表示“当前正在访问系统的人”。
- `UsernamePasswordToken` 是本次登录用的临时凭证对象。
- `new` 表示根据类这张“图纸”创建一个真实对象。
- `subject.login(...)` 把临时凭证交给 Shiro。

特别注意：代码执行到 `subject.login(...)` 后，不会立刻走下一行。Shiro 必须先完成后面的查用户和验密码；成功才回来，失败则抛异常。

### 第 5 站：Shiro 回调 Realm

Shiro 根据配置找到 [`UserRealm#doGetAuthenticationInfo`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/security/UserRealm.java)：

```java
String username = String.valueOf(token.getPrincipal());
User user = userService.findByUsername(username);
```

谁调用这个方法？Shiro，不是 Controller。

参数从哪里来？就是 Controller 创建的 `UsernamePasswordToken`。

它调用谁？`UserService`。

它最后 return 给谁？return 给 Shiro。

### 第 6 站：Service 做业务处理

[`UserService#findByUsername`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/service/UserService.java)：

```java
public User findByUsername(String username) {
    if (username == null || username.isBlank()) {
        return null;
    }
    return userMapper.selectByUsername(username.trim());
}
```

这层做两件小事：

1. 空用户名不查数据库。
2. `trim()` 去掉首尾空格，再交给 Mapper。

为什么不让 Realm 或 Controller 直接写 SQL？因为以后增加“账号是否过期、登录时间限制、登录审计”等规则时，都应该集中放在业务层，而不是散在接收请求的地方。

### 第 7 站：Mapper 接口为什么没有方法体也能执行

[`UserMapper.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/mapper/UserMapper.java) 只有：

```java
User selectByUsername(String username);
```

它是接口，确实没有 `{ ... }` 方法体。程序启动时，MyBatis 会临时生成一个“代理对象”（框架替你造出的实现对象）。Service 实际拿到的是这个代理对象。

MyBatis 用两个名字找到对应 XML：

```xml
<mapper namespace="com.pengzhipeng.miniruoyi.mapper.UserMapper">
<select id="selectByUsername">
```

- `namespace` 必须等于 Mapper 接口的完整类名。
- `id` 必须等于 Java 方法名。

两者对不上，运行时就会报找不到映射语句。

### 第 8 站：参数进入 SQL，H2 返回一行数据

[`UserMapper.xml`](./mini-ruoyi-login/src/main/resources/mapper/UserMapper.xml) 执行：

```sql
SELECT id, username, password_hash, salt, display_name,
       enabled, role_key, permission
FROM app_user
WHERE username = #{username}
```

`#{username}` 不是字符串拼接。MyBatis 会使用预编译参数，把 `admin` 安全地放进 `?` 的位置。启动窗口中能看到：

```text
Preparing: ... WHERE username = ?
Parameters: admin(String)
Total: 1
```

H2 在 [`data.sql`](./mini-ruoyi-login/src/main/resources/data.sql) 中找到一行 `admin`。MyBatis 根据列名和 Java 属性名把它装入一个 `User` 对象：

```text
数据库 password_hash → User.passwordHash
数据库 display_name  → User.displayName
数据库 role_key      → User.roleKey
```

`map-underscore-to-camel-case: true` 负责把下划线命名自动转换成 Java 的驼峰命名。

### 第 9 站：`User` 沿调用栈原路返回

这一步专门解决“方法和返回值混在一起”的问题：

```text
H2 查询出一行
  ↓ MyBatis 把这一行装成 User 对象
Mapper return User
  ↓
Service return User
  ↓
Realm 得到变量 user
```

每个方法暂停执行并等待下层结果。下层 `return` 后，上层才继续。像你让同学去档案室取资料：你在原地等；同学带资料回来；你再处理后面的事情。

### 第 10 站：Realm 检查状态，交出认证信息

Realm 先判断：

```java
if (user == null) { ... }
if (!user.isEnabled()) { ... }
```

然后返回：

```java
return new SimpleAuthenticationInfo(
        user,
        user.getPasswordHash(),
        ByteSource.Util.bytes(user.getSalt()),
        getName()
);
```

这里交给 Shiro 的四样东西是：完整用户对象、数据库密码哈希、盐、Realm 名称。

### 第 11 站：密码到底在哪里比较

密码不是 Mapper 比，也不是 Controller 比。真正比较的是 [`ShiroConfig.java`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/config/ShiroConfig.java) 配置的 `HashedCredentialsMatcher`：

```java
HashedCredentialsMatcher matcher = new HashedCredentialsMatcher("SHA-256");
matcher.setHashIterations(1024);
```

它把本次输入的 `admin123` 加上数据库中的盐 `mini-ruoyi-2026`，做 1024 次 SHA-256，然后和数据库里的 `password_hash` 比较。

```text
本次输入密码 + salt → 重复哈希 1024 次 → 结果 A
数据库 password_hash                         → 结果 B
A == B：成功；A != B：失败
```

数据库从未保存明文 `admin123`。这个方案适合教学并明显好于明文；真实生产系统应优先使用 BCrypt 或 Argon2 这类专门的慢密码算法，并启用 HTTPS。

### 第 12 站：Shiro 建立 Session

密码匹配成功后，`subject.login(...)` 正常结束。Shiro 把“当前人已经登录”保存在 Session 中，并给浏览器响应一个 HttpOnly Cookie：

```text
MINI_RUOYI_SESSION=<随机会话编号>
```

浏览器以后访问同一网站会自动带上它。Cookie 只放随机会话编号，不放用户密码。

### 第 13 站：Controller 创建安全返回对象

代码回到 `subject.login(...)` 的下一行：

```java
User user = (User) subject.getPrincipal();
return ResponseEntity.ok(
        ApiResponse.success("登录成功", new ProfileResponse(user))
);
```

为什么不直接把 `User` 返回给前端？因为 `User` 内有 `passwordHash` 和 `salt`。`ProfileResponse` 只复制可以公开的字段，相当于从内部完整档案中开一张安全证明。

Spring/Jackson 会把 Java 对象序列化（把 Java 对象转成 JSON 文字）：

```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "id": 1,
    "username": "admin",
    "displayName": "学习管理员",
    "role": "admin",
    "permission": "dashboard:view"
  }
}
```

登录响应表示“认证”已经成功。为了保证第一次点击登录时连“授权”也真实跑完，页面不会立刻展示这份数据，而是紧接着请求受保护的 `GET /api/profile`。Shiro 检查 `dashboard:view` 权限成功后，页面才使用第二次响应里的 `ProfileResponse`，隐藏登录区域并显示唯一主页。刷新页面时也走同一个 `/api/profile` 流程。

---

## 4. 数据一路“变形”的账本

你现在容易把“对象、变量、返回值”混淆，可以固定看这张表：

| 阶段 | 数据类型/形态 | 示例变量或内容 | 谁创建 | 交给谁 |
|---|---|---|---|---|
| 输入框 | HTML 文本 | `admin`、`admin123` | 用户输入 | 页面 JS |
| HTTP 正文 | 表单文字 | `username=admin&password=admin123` | 页面 JS | Controller 参数绑定 |
| Java 参数 | 两个 `String` | `username`、`password` | Spring | `login` 方法 |
| 临时凭证 | `UsernamePasswordToken` 对象 | 用户名 + 本次密码 | Controller | Shiro |
| SQL 参数 | `String` | `admin` | Service/Mapper | H2 |
| 数据库结果 | 表中的一行 | 8 个列值 | H2 | MyBatis |
| 内部对象 | `User` 对象 | 完整用户和密码哈希 | MyBatis | Mapper → Service → Realm |
| 认证信息 | `SimpleAuthenticationInfo` | User + 哈希 + 盐 | Realm | Shiro Matcher |
| 会话状态 | Shiro Session | 已登录的 User | Shiro | 后续请求 |
| 公开对象 | `ProfileResponse` | 5 个安全字段 | Controller | `ApiResponse` |
| 统一回执 | `ApiResponse<ProfileResponse>` | code/message/data | Controller | Spring/Jackson |
| HTTP 响应 | JSON 文字 | `{"code":0,...}` | Spring/Jackson | 浏览器 |

变量不是对象本身，可以把变量理解为“贴在对象上的标签”。

```java
User user = userService.findByUsername(username);
```

- `User`：类型/图纸。
- 右边方法返回的对象：内存中的真实用户档案。
- `user`：指向这个对象的变量标签。
- `=`：把右边结果的引用交给左边变量，不是数学上的相等判断。

---

## 5. 登录后的主页为什么受保护

页面刷新时会请求：

```text
GET /api/profile
Cookie: MINI_RUOYI_SESSION=...
```

页面把主页区域设成 `hidden` 只是一种界面显示效果，**不是安全措施**。真正保护数据的是后端过滤器和 `/api/profile` 的权限检查；别人即使手动改 HTML，也不能绕过后端的 401/403。

### 5.1 先认证：你是谁

`/api/profile` 匹配 `/api/** = jsonAuthc`。[`JsonAuthenticationFilter`](./mini-ruoyi-login/src/main/java/com/pengzhipeng/miniruoyi/security/JsonAuthenticationFilter.java) 检查：

```java
getSubject(request, response).isAuthenticated()
```

- Session 有效：放行到 Controller。
- 没有 Session：直接返回 HTTP 401，不进入 Controller。

### 5.2 再授权：你能做什么

即使已经登录，`profile()` 上还有：

```java
@RequiresPermissions("dashboard:view")
```

Shiro 会回调 Realm 的 `doGetAuthorizationInfo`，从当前 `User` 中取出：

```text
role = admin
permission = dashboard:view
```

这里的用户名和角色恰好都叫 `admin`，但它们不是同一个概念：用户名回答“你是哪一个账号”，角色回答“这个账号属于哪一类人”。

拥有相同权限才调用 `profile()`。这就是两个容易混的词：

- 认证 Authentication：确认你是谁，通常发生在登录。
- 授权 Authorization：确认你能做什么，通常发生在访问功能前。

这个项目只有一个主页，所以只保留一个权限；RuoYi 会把角色、菜单、按钮权限拆成多张表。

---

## 6. 三条失败支线和退出

### 6.1 密码错误

用户名 `admin`、密码错误时，Mapper 仍能查到 `User`。但是 Matcher 算出的哈希不同，于是 Shiro 抛出认证异常。正常 `return` 路线在这里中断，程序跳到 Controller 的 `catch`：

```java
catch (AuthenticationException exception) {
    return 401 + "用户名或密码错误";
}
```

对外不区分“用户名不存在”和“密码错误”，可以减少别人批量猜测有效账号的机会。

### 6.2 未登录访问主页接口

没有 Session 时，`JsonAuthenticationFilter` 直接返回：

```json
{"code":401,"message":"未登录或登录已过期","data":null}
```

此时断点不会进入 `AuthController#profile`，因为门卫已经拦住了。

### 6.3 已登录但没有权限

认证通过但缺少 `dashboard:view` 时，`@RequiresPermissions` 抛出 `AuthorizationException`，Controller 中的 `@ExceptionHandler` 把它变成 HTTP 403。

记忆方法：

- 401：还不知道你是谁，先登录。
- 403：知道你是谁，但你不能做这件事。

### 6.4 退出登录

`POST /api/logout` 调用：

```java
SecurityUtils.getSubject().logout();
```

Shiro 让 Session 失效，并要求浏览器删除 Cookie。拿旧 Cookie 再访问 `/api/profile`，仍会得到 401。

---

## 7. 只补这条链需要的 Java 基础

### 7.1 类、对象、变量

- 类：图纸，例如 `User` 类规定用户有哪些字段和方法。
- 对象：按图纸造出的实体，例如数据库那一行映射出的某个 `User`。
- 变量：指向对象的标签，例如 `user`。

### 7.2 参数和返回值

看这句：

```java
public User findByUsername(String username)
```

- 输入参数：调用者把一个 `String` 交进来。
- 返回类型：方法答应交回一个 `User`，也可能是 `null`。
- `return`：结束当前方法，把结果交回调用它的上一层。
- `void`：方法不交回业务结果。
- `null`：变量目前没有指向任何对象；它不是一个“内容为空的 User”。

### 7.3 字段、Getter、Setter

`User` 中的 `private String username` 是字段。`private` 表示外部不能直接乱改，所以通过：

```java
getUsername()
setUsername(...)
```

MyBatis 使用 Setter 把数据库结果填入对象，其他代码使用 Getter 读取。

### 7.4 接口和代理对象

`UserMapper` 是接口，只说明“能做什么”。MyBatis 在运行时生成代理对象，提供“具体怎样做”。这个代理对象收到 `selectByUsername` 调用后，去 XML 找 SQL。

所以你在调试时通常不能 Step Into 进入 Mapper 接口方法体，因为根本没有你写的方法体；应该在 Service 的下一行观察返回的 `User`，同时看控制台 SQL。

### 7.5 构造器注入

```java
public UserService(UserMapper userMapper) {
    this.userMapper = userMapper;
}
```

代码没有自己 `new UserMapper()`。Spring 启动时先准备 MyBatis 代理对象，再通过构造方法交给 `UserService`。这叫依赖注入：对象需要的合作者由容器送进来。

### 7.6 泛型只是说明“盒子里装什么”

```java
ApiResponse<ProfileResponse>
```

可以把 `ApiResponse` 想成统一快递盒，尖括号表示这次盒子里的 `data` 是 `ProfileResponse`。换一个接口也可以装别的类型。

### 7.7 异常是一条提前离开的路线

正常路线使用 `return` 一层层回去。发生异常时，当前方法后面的普通语句不再执行，程序沿调用栈向上寻找能处理它的 `catch` 或 `@ExceptionHandler`。

### 7.8 注解先按四类记

| 类别 | 本项目例子 | 谁读取 | 白话作用 |
|---|---|---|---|
| 身份标签 | `@RestController`、`@Service`、`@Configuration` | Spring | 告诉容器“这个类是什么角色” |
| 路由 | `@PostMapping`、`@GetMapping` | Spring MVC | 哪个 HTTP 请求进入哪个方法 |
| 参数绑定 | `@RequestParam` | Spring MVC | 请求数据装进哪个 Java 参数 |
| 权限 | `@RequiresPermissions` | Shiro/AOP | 调方法前必须先检查权限 |

注解不是按从上到下顺序执行的普通代码，而是给框架看的说明标签。

---

## 8. 每一层应该做什么、不应该做什么

| 层 | 应该做 | 不应该做 |
|---|---|---|
| Controller | 接收参数、调用流程、决定 HTTP/JSON 返回 | 直接写 SQL、堆大量业务规则 |
| Shiro/Realm | 认证、授权、把框架接到用户数据 | 写页面、拼 HTML |
| Service | 业务判断、编排 Mapper | 处理浏览器样式或直接写 HTTP 响应 |
| Mapper | 定义数据库动作、绑定 SQL | 做登录页面跳转 |
| Domain/DTO | 承载数据 | 主动控制完整业务流程 |
| Database | 保存和查询数据 | 理解 Java Controller |

分层不是为了多建文件，而是让每一层只承担一种变化。页面改版时不碰 SQL；换数据库时不改 Controller；加业务规则时主要看 Service。

---

## 9. 用断点亲手追一遍

不要一路 Step Into 到 Spring、Shiro、MyBatis 内部，否则会掉进几万行框架源码。正确方法是在自己的代码各站设断点，然后用 Resume 跳到下一站。

### 9.1 成功登录断点顺序

1. `AuthController#login` 方法第一行。
2. `subject.login(...)` 这一行：先观察 Token，再 Step Over。
3. `UserRealm#doGetAuthenticationInfo` 第一行。
4. `UserService#findByUsername` 第一行。
5. `userMapper.selectByUsername(...)` 的下一行。可以临时拆成两句，方便看返回值：

   ```java
   User user = userMapper.selectByUsername(username.trim());
   return user;
   ```

6. Realm 中 `return new SimpleAuthenticationInfo(...)` 之前。
7. 回到 Controller 中 `subject.login(...)` 的下一行。
8. Controller 最终 `return ResponseEntity.ok(...)` 之前。
9. 再刷新主页，在 `profile()` 第一行停一次，观察当前 principal。

每站只回答“小白四问”：

```text
谁调用我？
参数从哪里来？
我接下来调用谁？
我的 return 回给谁？
```

### 9.2 只学三个调试按钮

- Step Over：执行当前行，但不钻进框架内部。
- Step Into：只在你明确想进入自己的下一层方法时使用。
- Resume：继续运行到下一个已设置断点，最适合跨过框架代码。

### 9.3 再用错误密码跑一次

比较两次路线：错误密码仍会到 Mapper、仍会得到 `User`，但不会到 Controller 的“登录成功”返回处，而是进入 `catch`。这能让你真正看到异常如何改变流程。

---

## 10. 怎样启动和访问

### 10.1 在服务器启动

```bash
ssh pengzhipeng
cd /SSD2/pengzhipeng/backend/mini-ruoyi-login
./run.sh
```

看到下面文字就代表启动成功：

```text
Tomcat started on port 8081
Started MiniRuoYiApplication
```

当前 Shiro 2.2 与 Spring Boot 4 的组合可能在启动中打印若干 `Bean ... not eligible for auto-proxying` 的 `WARN`。它们是框架初始化顺序提示，不是启动失败；本项目的权限注解已经用错误权限做过 403 反向验证。你先看最后有没有 `Started MiniRuoYiApplication`，真正失败通常会出现 `ERROR`、`BUILD FAILURE` 或程序直接退出。

停止项目：在运行窗口按 `Ctrl + C`。

端口使用 `8081`，因为服务器的 `8080` 已经被其他程序占用。

### 10.2 浏览器访问

当前服务器配置已经按直接访问的需要监听 `0.0.0.0`，可以直接打开主机地址：

```text
http://222.201.56.105:8081/
```

如果以后把 `application.yml` 改回更安全的 `127.0.0.1`，请在你自己的电脑另开一个终端建立 SSH 隧道：

```bash
ssh -N -L 8081:127.0.0.1:8081 pengzhipeng
```

然后浏览器打开：

```text
http://127.0.0.1:8081/
```

现在的 `0.0.0.0` 只是为了方便你从浏览器直接访问；它会让教学账号和 Session 暴露在公网 HTTP 上。不要放真实密码或真实用户数据，学习结束后停止服务；生产环境还需要 HTTPS、CSRF、防暴力登录和更严格的 Cookie 策略。

如果公网地址偶尔显示 502，可以在自己电脑的终端建立隧道，绕过中间代理：

```bash
ssh -N -L 18081:127.0.0.1:8081 pengzhipeng
```

保持这个终端不要关闭，再打开 `http://127.0.0.1:18081/`。这里的 `18081` 是你电脑的本地端口，右边的 `8081` 才是服务器上的项目端口。

### 10.3 四个验收现象

1. `/` 能看到登录页。
2. 错误密码显示“用户名或密码错误”。
3. `admin/admin123` 登录后显示用户、角色和权限。
4. 退出后刷新，重新回到登录区。

H2 是内存数据库：停止项目后数据消失；下次启动时 `schema.sql` 和 `data.sql` 会自动重新建表、插入同一个教学账号。这是为了让你不用维护 MySQL，不代表生产数据库也这样工作。

---

## 11. 这个小项目和 RuoYi-master 怎样对应

| 最小项目 | RuoYi-master 中大致对应 | RuoYi 多出来的内容 |
|---|---|---|
| `MiniRuoYiApplication` | `RuoYiApplication` | 多模块启动和更多配置 |
| `AuthController` | `SysLoginController` | 验证码、注册、页面模型等 |
| `UserRealm` | `framework/shiro/realm/UserRealm` | 复杂角色、菜单、缓存、异常分类 |
| `UserService` | `SysLoginService` + `ISysUserService` | 黑名单、日志、重试次数、登录信息更新 |
| `UserMapper`/XML | `SysUserMapper`/XML | 部门、角色等关联查询 |
| `User` | `SysUser` | 大量系统字段和关联对象 |
| `ApiResponse` | `AjaxResult` | 更多工具方法和通用约定 |
| `ShiroConfig` | `framework/config/ShiroConfig` | 验证码、记住我、在线用户、踢出、CSRF、缓存 |
| H2 两个 SQL | `sql/ry_*.sql` + MySQL | 许多业务表和初始化数据 |
| 单个 `index.html` | templates + static | 完整后台 UI、菜单、插件 |

所以这个小项目不是“另一套知识”。它就是把 RuoYi 的登录主干留下，把菜单、字典、定时任务、生成器、监控、多模块等支线暂时剪掉。

建议学习顺序：

```text
先把这个项目成功登录流程讲顺
→ 再去 RuoYi 找同名/对应文件
→ 一次只增加一条支线，例如登录日志
→ 最后再看角色菜单、缓存、验证码等复杂功能
```

---

## 12. 小练习：按难度做

### 第一级：能看懂

1. 在 `public User findByUsername(String username)` 中，指出返回类型、方法名、参数类型、参数变量名。
2. 不看上文，写出登录去程的 8 个站点。
3. 说出真正比较密码的类，不要回答 Controller 或 Mapper。
4. 解释 `User` 为什么不能直接返回前端。

完成标准：你能用自己的话讲，而不是只背箭头。

### 第二级：能调试

1. 正确密码登录时，在 Service 里记下 Mapper 返回的 `User` 各字段。
2. 错误密码登录，记录最后进入了哪个自己的方法，以及为什么没有走成功 return。
3. 登录前后分别观察 `subject.isAuthenticated()`。
4. 看控制台 SQL，找到 `?` 对应的实际参数。

完成标准：你能指出数据此刻在变量、对象、SQL、JSON 中是哪一种形态。

### 第三级：能改一条完整链

给用户增加一个 `email` 字段，必须依次修改：

```text
schema.sql 建字段
→ data.sql 填数据
→ User 加字段/getter/setter
→ UserMapper.xml 查询该列
→ ProfileResponse 选择是否公开
→ index.html 显示
```

完成标准：不是只改页面，而是数据库到页面每一站都能说清楚。

### 思考题

如果 Controller 绕过 Service 直接调用 Mapper，现在看起来少一层；将来加入账号锁定、登录时间限制、登录审计时，会出现什么问题？

提示：同一条业务规则可能散落在多个 Controller 中，难以复用，也容易漏掉。

---

## 13. 一页复习卡

### 去程

```text
表单 → POST /api/login → Shiro 放行 → Controller 参数
→ UsernamePasswordToken → subject.login
→ Realm → Service → Mapper 接口 → Mapper XML → H2
```

### 回程

```text
H2 一行 → User → Mapper return → Service return → Realm
→ AuthenticationInfo → Matcher 验密 → Session
→ ProfileResponse → ApiResponse → JSON → 页面
```

### 登录后紧接着的授权请求

```text
页面 → GET /api/profile + Session Cookie → jsonAuthc 确认已登录
→ @RequiresPermissions → Realm 提供 dashboard:view → Controller
→ ProfileResponse → ApiResponse → JSON → 页面显示主页
```

### 五个最关键对象

- `UsernamePasswordToken`：本次输入的临时登录凭证。
- `User`：数据库内部完整用户。
- `SimpleAuthenticationInfo`：Realm 交给 Shiro 的认证资料。
- `ProfileResponse`：对前端公开的安全用户资料。
- `ApiResponse<T>`：统一 JSON 外包装。

### 常见问题从哪里查

| 现象 | 第一检查点 |
|---|---|
| 404 | Controller 的路径和 HTTP 方法是否匹配 |
| 401 | Session Cookie 是否存在，Shiro 过滤链是否放行登录接口 |
| 403 | `@RequiresPermissions` 与数据库权限字符串是否相同 |
| 500、表不存在 | `schema.sql` 是否执行，H2 配置是否正确 |
| 查不到用户 | SQL 参数、`data.sql`、Mapper XML |
| `User` 字段是空 | 数据库列名与 Java 属性映射 |
| 端口占用 | `application.yml` 的 8081 是否被其他程序使用 |
| 页面说请求失败 | 后端是否仍在运行；必要时使用 SSH 隧道 |

最后只记一句：**请求是向下交任务，`return` 是把结果沿调用栈向上交回来；Shiro 在中间负责确认“你是谁”和“你能做什么”。**
