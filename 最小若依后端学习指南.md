# 从一次登录看懂整个后端：无 Shiro 的最小 RuoYi 学习指南

> 教学账号：`admin / admin123`

这个版本故意移除了 Apache Shiro，只保留最容易理解的一条后端链路：

```text
浏览器
  ↓ HTTP
Controller
  ↓ 调用
Service
  ↓ 调用
Mapper
  ↓ SQL
H2 数据库
```

登录成功后，Controller 使用 Java 原生 `HttpSession` 保存当前用户。因此你现在可以先把整个项目理解成最清晰的三层结构，而不用先理解一个安全框架的内部回调。

## 1. 项目结构

```text
mini-ruoyi-login/
├── pom.xml
├── run.sh
├── javac21.sh
└── src/main/
    ├── java/com/pengzhipeng/miniruoyi/
    │   ├── MiniRuoYiApplication.java
    │   ├── common/ApiResponse.java
    │   ├── controller/AuthController.java
    │   ├── domain/User.java
    │   ├── dto/ProfileResponse.java
    │   ├── mapper/UserMapper.java
    │   └── service/UserService.java
    └── resources/
        ├── application.yml
        ├── schema.sql
        ├── data.sql
        ├── mapper/UserMapper.xml
        └── static/index.html
```

最重要的 6 个文件：

```text
index.html
    ↓
AuthController.java
    ↓
UserService.java
    ↓
UserMapper.java
    ↓
UserMapper.xml
    ↓
H2
```

## 2. 每一层到底负责什么

### Controller：处理 HTTP

`AuthController` 负责：

- 接收 `/api/login`
- 接收 `/api/profile`
- 接收 `/api/logout`
- 从 HTTP 请求读取参数
- 调用 Service
- 返回 JSON
- 使用 `HttpSession` 保存和读取登录用户

它不应该直接写 SQL。

### Service：处理业务

`UserService` 负责：

- 整理用户名
- 查询用户
- 检查账号是否启用
- 校验密码
- 返回认证成功后的 `User`

这里是最适合放“业务规则”的地方。

### Mapper：负责数据库访问

`UserMapper` 只有一个动作：

```java
User selectByUsername(String username);
```

它告诉 MyBatis：“我要按照用户名查询用户。”

真正的 SQL 在：

```text
resources/mapper/UserMapper.xml
```

所以：

```text
Service
  ↓
Mapper 方法
  ↓
MyBatis
  ↓
XML 中的 SQL
  ↓
H2
```

### Domain：数据库数据对应的 Java 对象

`User.java` 对应数据库中的 `app_user` 表。

它包含：

```text
id
username
passwordHash
salt
displayName
enabled
roleKey
permission
```

### DTO：返回给前端的数据

`ProfileResponse` 只暴露安全字段，不把密码哈希和盐返回给浏览器。

### ApiResponse：统一 JSON 格式

Controller 最终返回类似：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {}
}
```

## 3. 一次登录完整走一遍

假设浏览器输入：

```text
username = admin
password = admin123
```

浏览器发送：

```text
POST /api/login
```

### 第一步：进入 Controller

Spring MVC 根据：

```java
@PostMapping("/login")
```

找到：

```java
AuthController.login(...)
```

Controller 拿到用户名和密码。

### 第二步：Controller 调用 Service

```java
User user = userService.authenticate(username, password);
```

现在进入 Service。

### 第三步：Service 调用 Mapper

Service 先：

```java
findByUsername(username)
```

然后调用：

```java
userMapper.selectByUsername(username)
```

### 第四步：MyBatis 执行 SQL

MyBatis 找到：

```text
UserMapper.xml
```

执行类似：

```sql
SELECT * FROM app_user WHERE username = #{username}
```

H2 返回数据库的一行数据。

### 第五步：MyBatis 把数据库行变成 User

```text
数据库一行
    ↓
MyBatis
    ↓
User 对象
```

然后：

```text
Mapper → Service
```

### 第六步：Service 校验密码

数据库中保存的不是明文密码，而是：

```text
盐 + SHA-256 迭代 1024 次
```

Service 对用户输入的 `admin123` 做同样的计算，然后与数据库中的 `password_hash` 比较。

一致：登录成功。

不一致：返回 401。

### 第七步：Controller 创建 Session

登录成功后：

```java
session.setAttribute("LOGIN_USER", user);
```

这里非常重要。

它表示：

```text
浏览器
  ↓
Session Cookie
  ↓
服务器找到对应 HttpSession
  ↓
HttpSession 中保存 User
```

以后浏览器再次请求 `/api/profile`，服务器就可以通过 Session 知道“这个请求是谁发来的”。

## 4. profile 是怎么工作的

浏览器请求：

```text
GET /api/profile
```

进入 Controller：

```java
User user = currentUser(session);
```

如果 Session 中没有用户：

```text
401 未登录
```

如果存在用户，并且拥有：

```text
 dashboard:view
```

就返回：

```text
200 + ProfileResponse
```

所以现在的权限判断非常直观：

```text
Request
  ↓
Controller
  ↓
HttpSession
  ↓
User
  ↓
permission
  ↓
允许 / 拒绝
```

## 5. logout

退出登录非常简单：

```java
session.invalidate();
```

也就是直接销毁当前 Session。

之后再次访问 `/api/profile`，服务器就找不到登录用户，于是返回 401。

## 6. 现在这个项目为什么更适合学习

原来的版本是：

```text
Controller
  ↓
Shiro Subject
  ↓
SecurityManager
  ↓
Realm
  ↓
Service
  ↓
Mapper
```

这套结构是真实的安全框架结构，但对于第一次理解三层架构来说，会增加大量“框架内部跳转”。

现在变成：

```text
Controller
  ↓
Service
  ↓
Mapper
  ↓
Database
```

登录状态单独使用：

```text
HttpSession
```

这样你可以先彻底理解：

```text
HTTP
 ↓
Controller
 ↓
Service
 ↓
Mapper
 ↓
SQL
 ↓
Database
 ↓
Java Object
 ↓
JSON
```

等这条链路完全理解之后，再把 Shiro 加回来，你会很容易看懂它到底是在这条链路的什么位置提供了什么能力。

## 7. 推荐学习顺序

不要现在同时学所有文件。

按这个顺序看：

```text
1. index.html
      ↓
2. AuthController.login()
      ↓
3. UserService.authenticate()
      ↓
4. UserService.findByUsername()
      ↓
5. UserMapper.selectByUsername()
      ↓
6. UserMapper.xml
      ↓
7. data.sql
      ↓
8. 返回 User
      ↓
9. HttpSession
      ↓
10. /api/profile
```

如果这条线能自己讲出来，你基本就理解了这个项目的核心后端架构。
