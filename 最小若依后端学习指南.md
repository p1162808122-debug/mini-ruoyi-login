# 从一次登录看懂整个后端：MySQL 版最小 RuoYi 学习指南

> 教学账号：`admin / admin123`

这个项目现在只保留最容易理解的一条后端链路：

```text
浏览器
  ↓ HTTP
Controller
  ↓ 调用
Service
  ↓ 调用
Mapper
  ↓ MyBatis
MySQL
```

登录成功后，Controller 使用 Java 原生 `HttpSession` 保存当前用户。

## 1. 项目结构

```text
mini-ruoyi-login/
├── pom.xml
├── run.sh
├── javac21.sh
├── docker-compose.yml
└── src/main/
    ├── java/com/pengzhipeng/miniruoyi/
    │   ├── MiniRuoYiApplication.java
    │   ├── common/ApiResponse.java
    │   ├── controller/AuthController.java
    │   ├── domain/User.java
    │   ├── dto/ProfileResponse.java
    │   ├── mapper/UserMapper.java
    │   └── service/UserService.java
    └── resources/
        ├── application.yml
        ├── schema.sql
        ├── data.sql
        ├── mapper/UserMapper.xml
        └── static/index.html
```

最重要的调用链：

```text
index.html
    ↓
AuthController.java
    ↓
UserService.java
    ↓
UserMapper.java
    ↓
UserMapper.xml
    ↓
MySQL
```

## 2. 每一层负责什么

### Controller：处理 HTTP

`AuthController` 负责：

- 接收 `/api/login`
- 接收 `/api/profile`
- 接收 `/api/logout`
- 读取前端参数
- 调用 Service
- 返回 JSON
- 使用 `HttpSession` 保存和读取登录用户

Controller 不直接写 SQL。

### Service：处理业务

`UserService` 负责：

- 整理用户名
- 查询用户
- 检查账号是否启用
- 校验密码
- 返回认证成功后的 `User`

### Mapper：负责数据库访问

`UserMapper` 定义：

```java
User selectByUsername(String username);
```

真正的 SQL 在：

```text
resources/mapper/UserMapper.xml
```

完整过程：

```text
Service
  ↓
Mapper 方法
  ↓
MyBatis
  ↓
UserMapper.xml 中的 SQL
  ↓
MySQL
```

### Domain：数据库数据对应的 Java 对象

`User.java` 对应 MySQL 中的 `app_user` 表。

主要字段：

```text
id
username
passwordHash
salt
displayName
enabled
roleKey
permission
```

### DTO：返回给前端的数据

`ProfileResponse` 只返回前端需要的字段，不返回密码哈希和盐。

### ApiResponse：统一 JSON 格式

Controller 返回的数据类似：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {}
}
```

## 3. MySQL 配置在哪里

数据库连接配置在：

```text
src/main/resources/application.yml
```

默认连接：

```text
jdbc:mysql://localhost:3306/mini_ruoyi
```

项目也支持环境变量：

```text
MYSQL_URL
MYSQL_USERNAME
MYSQL_PASSWORD
```

`schema.sql` 负责创建表结构，`data.sql` 负责插入教学测试数据。

`docker-compose.yml` 可以用于启动项目需要的 MySQL 服务。

## 4. 一次登录完整走一遍

假设输入：

```text
username = admin
password = admin123
```

浏览器发送：

```text
POST /api/login
```

### 第一步：进入 Controller

Spring MVC 根据：

```java
@PostMapping("/login")
```

找到：

```java
AuthController.login(...)
```

### 第二步：Controller 调用 Service

```java
User user = userService.authenticate(username, password);
```

### 第三步：Service 调用 Mapper

```java
userMapper.selectByUsername(username)
```

### 第四步：MyBatis 执行 SQL

MyBatis 找到：

```text
UserMapper.xml
```

然后去 MySQL 查询 `app_user` 表。

可以理解为：

```text
Java Mapper 方法
      ↓
MyBatis
      ↓
SQL
      ↓
MySQL
```

### 第五步：MyBatis 返回 User

```text
MySQL 查询结果
      ↓
MyBatis
      ↓
User 对象
      ↓
Service
```

### 第六步：Service 校验密码

数据库保存的是密码哈希和盐。

当前教学项目使用：

```text
盐 + SHA-256，连续计算 1024 次
```

校验正确就返回 `User`，错误就登录失败。

### 第七步：Controller 保存 Session

登录成功后：

```java
session.setAttribute("LOGIN_USER", user);
```

过程：

```text
浏览器
  ↓ Session Cookie
服务器找到 HttpSession
  ↓
读取 LOGIN_USER
```

## 5. profile 怎么工作

浏览器请求：

```text
GET /api/profile
```

Controller 从 Session 中读取：

```java
User user = currentUser(session);
```

没有用户：

```text
401 未登录
```

如果用户拥有：

```text
dashboard:view
```

就返回用户信息。

权限判断可以理解为：

```text
Request
  ↓
Controller
  ↓
HttpSession
  ↓
User
  ↓
permission
  ↓
允许 / 拒绝
```

## 6. logout 怎么工作

退出登录：

```java
session.invalidate();
```

也就是销毁当前 Session。

之后再次访问 `/api/profile`，服务器找不到登录用户，就返回 401。

## 7. 推荐学习顺序

```text
1. index.html
      ↓
2. AuthController.login()
      ↓
3. UserService.authenticate()
      ↓
4. UserService.findByUsername()
      ↓
5. UserMapper.selectByUsername()
      ↓
6. UserMapper.xml
      ↓
7. MySQL / schema.sql / data.sql
      ↓
8. 返回 User
      ↓
9. HttpSession
      ↓
10. /api/profile
```

先把这一条链路理解清楚：

```text
前端请求
  ↓
Controller
  ↓
Service
  ↓
Mapper
  ↓
MyBatis
  ↓
MySQL
  ↓
User
  ↓
JSON 返回前端
```
